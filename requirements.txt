# Curated requirements for this repository (tested with Python 3.8+).
#
# Notes:
# - For GPU support, install `torch` following instructions at https://pytorch.org
#   (the generic `pip install torch` may install a CPU-only build). Example GPU
#   install commands depend on CUDA version and platform.
# - The version constraints are conservative to improve reproducibility while
#   remaining flexible. Adjust versions if you need newer features.

# ---- Core scientific stack ----
numpy>=1.23,<2.0
pandas>=1.5,<2.0
scipy>=1.9,<2.0
matplotlib>=3.5,<4.0
seaborn>=0.12,<1.0

# ---- Image processing ----
opencv-python>=4.6,<5.0
Pillow>=9.0,<12.0
scikit-image>=0.19,<1.0

# ---- Machine learning ----
scikit-learn>=1.1,<2.0
tqdm>=4.64,<5.0

# ---- PyTorch (choose CPU or GPU build) ----
# Recommended: follow https://pytorch.org/get-started/locally/ to pick the
# correct `torch`/`torchvision`/`torchaudio` wheel for your platform.
# The following are conservative version constraints; install command may differ
# for GPU systems.
torch>=2.0.1,<3.0
torchvision>=0.15.2,<1.0
torchaudio>=2.0.2,<3.0

# ---- Data augmentation / model helpers ----
albumentations>=1.3.0,<2.0
timm>=0.9.2,<1.0

# ---- Excel / IO ----
openpyxl>=3.0.10,<4.0

# ---- Notebooks / dev ----
jupyter>=1.0.0
ipykernel>=6.0
